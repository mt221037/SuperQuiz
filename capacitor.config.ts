import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.ac.fhstp.sophie.superquiz202503',
  appName: 'SuperQuiz',
  webDir: 'www'
};

export default config;
